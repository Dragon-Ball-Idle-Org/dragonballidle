# name: Build, push and deploy

# on:
#   push:
#     branches: [main]

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Criar .env.production para o Vite
#         working-directory: ./dragonballdle.site
#         run: |
#           cat <<EOF > .env.production
#           VITE_DAILY_SECRET=${{ secrets.VITE_DAILY_SECRET }}
#           VITE_FORCE_YMD=${{ secrets.VITE_FORCE_YMD }}
#           EOF

#       - name: Login no registry
#         uses: docker/login-action@v3
#         with:
#           username: ${{ secrets.REGISTRY_USERNAME }}
#           password: ${{ secrets.REGISTRY_PASSWORD }}

#       - name: Build image
#         run: |
#           docker build \
#             -f dragonballdle.site/Dockerfile \
#             -t ${{ secrets.REGISTRY_IMAGE }}:latest \
#             -t ${{ secrets.REGISTRY_IMAGE }}:${{ github.sha }} \
#             .

#       - name: Push image
#         run: |
#           docker push ${{ secrets.REGISTRY_IMAGE }}:latest
#           docker push ${{ secrets.REGISTRY_IMAGE }}:${{ github.sha }}

#   deploy-edge-functions:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - uses: supabase/setup-cli@v1
#         with:
#           version: latest

#       - name: Deploy Edge Functions
#         run: supabase functions deploy --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
#         env:
#           SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

#   deploy:
#     runs-on: ubuntu-latest
#     needs: [build-and-push, deploy-edge-functions]
#     steps:
#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1.2.0
#         with:
#           host: ${{ secrets.SSH_HOST }}
#           username: ${{ secrets.SSH_USER }}
#           key: ${{ secrets.SSH_KEY }}
#           script: |
#             docker login -u ${{ secrets.REGISTRY_USERNAME }} -p ${{ secrets.REGISTRY_PASSWORD }}
#             docker pull ${{ secrets.REGISTRY_IMAGE }}:latest
#             docker stop dragonballdle || true
#             docker rm dragonballdle || true
#             docker run --build-arg ENV=production -d --name dragonballdle \
#               -p 80:80 \
#               -p 443:443 \
#               -v /etc/letsencrypt:/etc/letsencrypt \
#               ${{ secrets.REGISTRY_IMAGE }}:latest
